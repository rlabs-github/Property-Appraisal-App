{
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "firebase": "^11.8.1"
  },
  "scripts": {
    "mock-api": "json-server --watch db.json --routes routes.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
